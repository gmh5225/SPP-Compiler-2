mod std::func;


cls A {
    a: Num;
    b: Num;
}


sup B for A {
    fn new(a: Num, b: Num) -> Self {
        ret Self { a, b };
    }
}


sup C for A {
    fn test(self: &Self) -> Num {
        ret self.a + self.b;
    }
}

